spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bibliotheek
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: true
    open-in-view: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
